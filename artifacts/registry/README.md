# Artifact registry

Dataset and model manifests are generated here only after all release gates pass.
Artifacts are immutable and must use a new version instead of overwriting an
existing model or dataset generation.

No v2 registry manifest exists yet because the current dataset is quarantined.
